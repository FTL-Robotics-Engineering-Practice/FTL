# 🤖 Занятие 4: Математическое движение робота

## 🎯 Цель занятия
Изучить математику движения, создав робота с реалистичными параметрами. Повторить и закрепить ООП.

## 📋 Что повторяем:
- **Занятие 2:** Классы, методы, атрибуты
- **Занятие 3:** Методы с return, геттеры/сеттеры, цепочки вызовов

---

## 🏗️ Задание 0: Базовый класс Robot (20 мин)
**Повторяем:** Все основы ООП

### Что должно быть в классе:

**Атрибуты:**
- `name` - имя робота
- `x, y` - координаты (начальные 0, 0)  
- `direction` - направление в градусах (начальное 0)
- `speed` - скорость единиц/сек (начальная 0)
- `dt` - временной шаг для расчётов (0.1 сек)

**Обязательные методы:**
- `__init__(self, name)` - конструктор
- `get_position(self)` - возвращает кортеж `(x, y)`
- `get_direction(self)` - возвращает направление в градусах
- `get_speed(self)` - возвращает текущую скорость
- `set_direction(self, new_direction)` - устанавливает направление (0-360°)
- `set_speed(self, new_speed)` - устанавливает скорость (0-100)
- `info(self)` - выводит всю информацию о роботе

**Как должно работать:**
```python
# Создание робота
robot = Robot("Альфа")
print(robot.info())  # Альфа: позиция (0, 0), направление 0°, скорость 0

# Настройка робота
robot.set_direction(45)
robot.set_speed(10)
print(robot.get_position())    # (0, 0)
print(robot.get_direction())   # 45
print(robot.get_speed())       # 10
```

### Ячейка для проверки задания 0:
```python
# ПРОВЕРКА: Создайте робота и протестируйте все методы
robot = Robot("Тестер")

# Проверим геттеры
print("Начальная позиция:", robot.get_position())
print("Начальное направление:", robot.get_direction()) 
print("Начальная скорость:", robot.get_speed())

# Проверим сеттеры
robot.set_direction(90)
robot.set_speed(25)
print("После изменений:", robot.info())

# Проверим ограничения
robot.set_speed(150)  # Должно быть ограничено до 100
robot.set_direction(400)  # Должно быть приведено к 0-360°
print("После неправильных значений:", robot.info())
```

---

## ⏱️ ЧАС 1: Направление и шаги произвольной длины

### **Задание 1.1: Метод move_forward(distance) (20 мин)**
**Повторяем:** Методы с параметрами, математические вычисления

**Задача:** Добавить метод, который двигает робота вперёд по текущему направлению

**Математика для детей:**
```
Направления:
0° = восток (→), 90° = север (↑), 180° = запад (←), 270° = юг (↓)

Формулы:
import math
радианы = math.radians(direction)
новая_x = старая_x + distance * math.cos(радианы)
новая_y = старая_y + distance * math.sin(радианы)
```

### Ячейка для проверки задания 1.1:
```python
# ПРОВЕРКА: Движение в разные стороны
robot = Robot("Ходок")

# Движение на восток
robot.set_direction(0)
robot.move_forward(10)
print("После движения на восток:", robot.get_position())  # (10, 0)

# Движение на север  
robot.set_direction(90)
robot.move_forward(10)
print("После движения на север:", robot.get_position())   # (10, 10)

# Движение по диагонали
robot.set_direction(45)  # северо-восток
robot.move_forward(10)
print("После диагонального движения:", robot.get_position())  # примерно (17.07, 17.07)
```

### **Задание 1.2: Метод turn(angle) (15 мин)**
**Повторяем:** Изменение атрибутов, работа с числами

**Задача:** Добавить метод для поворота на заданный угол (положительный - против часовой, отрицательный - по часовой)

### Ячейка для проверки задания 1.2:
```python
# ПРОВЕРКА: Повороты
robot = Robot("Поворотник")
print("Начальное направление:", robot.get_direction())  # 0

robot.turn(90)  # поворот против часовой на 90°
print("После поворота на 90°:", robot.get_direction())  # 90

robot.turn(-45)  # поворот по часовой на 45°
print("После поворота на -45°:", robot.get_direction())  # 45

robot.turn(400)  # большой поворот
print("После большого поворота:", robot.get_direction())  # должно быть в пределах 0-360°
```

### **Задание 1.3: Рисование фигур (25 мин)**
**Повторяем:** Циклы, вызовы методов

**Задача:** Создать метод `draw_square(side_length)`, который рисует квадрат

### Ячейка для проверки задания 1.3:
```python
# ПРОВЕРКА: Рисование квадрата
robot = Robot("Художник")
print("Стартовая позиция:", robot.get_position())

robot.draw_square(20)
print("Финальная позиция:", robot.get_position())  # Должен вернуться в (0, 0)

# Проследим путь робота по точкам
robot = Robot("Следопыт")
robot.draw_square(15)  # В методе добавьте print для каждой вершины квадрата
```

---

## ⏱️ ЧАС 2: Скорость и временной шаг

### **Задание 2.1: Метод update() (20 мин)**
**Повторяем:** Использование атрибутов объекта, математика

**Задача:** Создать метод `update()`, который двигает робота на расстояние `speed * dt`

**Математика:**
```
За каждый кадр робот проходит: speed * dt
Если speed = 50 единиц/сек, dt = 0.1 сек
То за кадр = 50 * 0.1 = 5 единиц
```

### Ячейка для проверки задания 2.1:
```python
# ПРОВЕРКА: Движение со скоростью
robot = Robot("Скоростник")
robot.set_direction(0)  # на восток
robot.set_speed(20)     # 20 единиц/сек

print("Начальная позиция:", robot.get_position())

# Симуляция 5 кадров движения
for i in range(5):
    robot.update()
    print(f"Кадр {i+1}: {robot.get_position()}")

# За 5 кадров при dt=0.1 и speed=20 должен пройти 10 единиц по X
```

### **Задание 2.2: Безопасные сеттеры (20 мин)**
**Повторяем:** Геттеры/сеттеры, условия, контроль данных

**Задача:** Улучшить сеттеры с проверками:
- `set_speed(new_speed)` - ограничить 0-100
- `set_direction(new_direction)` - приводить к диапазону 0-360°

### Ячейка для проверки задания 2.2:
```python
# ПРОВЕРКА: Безопасные сеттеры
robot = Robot("Осторожный")

# Тестируем ограничения скорости
robot.set_speed(150)  # слишком много
print("Скорость после 150:", robot.get_speed())  # должно быть 100

robot.set_speed(-10)  # отрицательная
print("Скорость после -10:", robot.get_speed())  # должно быть 0

# Тестируем нормализацию направления  
robot.set_direction(450)  # больше 360
print("Направление после 450°:", robot.get_direction())  # должно быть 90

robot.set_direction(-90)  # отрицательное
print("Направление после -90°:", robot.get_direction())  # должно быть 270
```

### **Задание 2.3: Симуляция времени (20 мин)**
**Повторяем:** Циклы, вызовы методов, накопление данных

**Задача:** Создать метод `simulate(duration)`, который симулирует движение робота в течение заданного времени

### Ячейка для проверки задания 2.3:
```python
# ПРОВЕРКА: Симуляция движения во времени
robot = Robot("Путешественник")
robot.set_direction(45)  # северо-восток
robot.set_speed(30)      # 30 единиц/сек

print("До симуляции:", robot.get_position())
robot.simulate(2.0)  # 2 секунды движения
print("После 2 секунд:", robot.get_position())

# Должен пройти 60 единиц по диагонали (30 * 2)
# По X и Y примерно по 42.4 единицы (60/√2)
```

---

## ⏱️ ЧАС 3: Угловая скорость и сложные движения

### **Задание 3.1: Добавляем angular_speed (15 мин)**
**Повторяем:** Добавление новых атрибутов

**Задача:** Добавить атрибут `angular_speed` (градусы/сек) и геттер/сеттер с ограничением ±180°/сек

### **Задание 3.2: Поворот в update() (20 мин)**
**Повторяем:** Изменение состояния объекта

**Задача:** Дополнить метод `update()` поворотом робота

**Математика:**
```
новое_направление = старое_направление + angular_speed * dt
Потом привести к диапазону 0-360°
```

### Ячейка для проверки задания 3.2:
```python
# ПРОВЕРКА: Поворот во время движения
robot = Robot("Вертун")
robot.set_direction(0)
robot.set_speed(20)
robot.set_angular_speed(90)  # поворот 90°/сек

print("Начальное направление:", robot.get_direction())
for i in range(10):
    robot.update()
    print(f"Кадр {i+1}: направление {robot.get_direction():.1f}°, позиция {robot.get_position()}")

# Робот должен поворачиваться и двигаться по дуге
```

### **Задание 3.3: Движение по окружности (25 мин)**
**Повторяем:** Сложные математические расчёты

**Задача:** Создать метод `move_in_circle(radius, period)` для движения по кругу

**Математика:**
```
Для окружности радиусом R за время T:
длина_окружности = 2 * π * R
linear_speed = длина_окружности / T  
angular_speed = 360° / T
```

### Ячейка для проверки задания 3.3:
```python
# ПРОВЕРКА: Движение по кругу
robot = Robot("Кружащийся")
robot.move_in_circle(radius=20, period=4.0)  # круг радиусом 20 за 4 секунды

print("Настройки для круга:")
print("Скорость:", robot.get_speed())
print("Угловая скорость:", robot.get_angular_speed())

# Симуляция полного круга
robot.simulate(4.0)
print("После полного круга:", robot.get_position())  # должен вернуться близко к началу
```

### **⭐ Звёздочка: Спиральное движение**
**Для продвинутых:** Создать метод `spiral_movement()`, где скорость постепенно увеличивается

### Ячейка для проверки спирали:
```python
# ЗВЁЗДОЧКА: Спираль
robot = Robot("Спиральный")
robot.spiral_movement()

# Проследим как увеличивается радиус
for i in range(20):
    robot.update()
    distance_from_center = (robot.get_position()[0]**2 + robot.get_position()[1]**2)**0.5
    print(f"Кадр {i+1}: радиус {distance_from_center:.1f}")
```

---

## 🎯 Итоговые возможности робота

✅ Движение в любом направлении (градусы)  
✅ Шаги произвольной длины  
✅ Движение с заданной скоростью  
✅ Поворот с угловой скоростью *(в конце урока)*  
✅ Симуляция движения во времени  
✅ Рисование геометрических фигур  
✅ Движение по окружности  
✅ Безопасные ограничения параметров  

## 📝 Финальное испытание
Создать робота, который рисует правильный шестиугольник, двигаясь с постоянной скоростью 25 ед/сек, делая паузы на углах.