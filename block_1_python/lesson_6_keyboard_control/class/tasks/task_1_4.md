# ⌨️ Задание 1.4: Клавиатурное управление скоростями (18 мин)

**Повторяем:** Обработка клавиатуры в Pygame, условные операторы, управление состоянием.

Пришло время взять управление в свои руки! Вместо фиксированных скоростей мы будем менять их с помощью клавиш W, A, S, D.

---

## 🧠 Теория: Управление через скорости

### Схема управления

- **W** — движение вперёд (`linear_speed = 150`)
- **S** — движение назад (`linear_speed = -100`)
- **A** — поворот влево (`angular_speed = 120`)
- **D** — поворот вправо (`angular_speed = -120`)
- **W + S одновременно** — конфликт → `linear_speed = 0`
- **Отпустили все клавиши** — все скорости = 0 (робот останавливается)

---

### Важный принцип: Обнуление скоростей

**Каждый кадр** мы должны:
1. Сначала **обнулить** скорости
2. Затем **проверить** нажатые клавиши
3. Если клавиша нажата — установить соответствующую скорость

**Почему так?** 

Если клавиша не нажата, скорость должна быть 0. Иначе робот будет продолжать движение после отпускания клавиши (как будто есть инерция).

---

### Проверка состояния клавиш

**Сигнатура функции:**
```python
pygame.key.get_pressed() -> sequence
```

**Параметры:** нет

**Возвращает:** sequence (словарь-подобный объект), где можно проверить состояние любой клавиши

**Константы клавиш:**
- `pygame.K_w` — клавиша W
- `pygame.K_a` — клавиша A
- `pygame.K_s` — клавиша S
- `pygame.K_d` — клавиша D
- `pygame.K_LSHIFT` — левый Shift
- `pygame.K_SPACE` — пробел
- И другие: `K_UP`, `K_DOWN`, `K_LEFT`, `K_RIGHT` и т.д.

**Как использовать:**
```python
keys = pygame.key.get_pressed()

if keys[pygame.K_w]:
    # Клавиша W нажата прямо сейчас
    linear_speed = 150
```

Это проверяется **каждый кадр**. Если клавиша зажата — условие True, если отпущена — False.

**Разница от событий:**
- События (`pygame.KEYDOWN`) — срабатывают один раз при нажатии
- `get_pressed()` — проверяет состояние каждый кадр (подходит для плавного движения)

---

### Обработка конфликтов: W + S

Что если игрок нажмёт W и S одновременно? Обе клавиши означают движение в противоположных направлениях.

**Решение:** Если обе клавиши нажаты — скорость = 0 (робот не движется).

```python
if keys[pygame.K_w] and keys[pygame.K_s]:
    linear_speed = 0
elif keys[pygame.K_w]:
    linear_speed = 150
elif keys[pygame.K_s]:
    linear_speed = -100
```

**Важно:** Проверка конфликта должна быть **первой**!

---

## 📝 Практическое задание

### Шаг 1: Уберите временные значения скоростей

**Удалите или закомментируйте** эти строки:

```python
# ВРЕМЕННО: для проверки
# linear_speed = 50
# angular_speed = 30
```

Теперь скорости остаются = 0, как мы задали в начале программы.

---

### Шаг 2: Обнуление скоростей в начале кадра

В игровом цикле, **после** обработки событий и вычисления `dt`, но **до** проверки клавиш:

```python
# TODO: Обнуляем скорости (если клавиши не нажаты, робот стоит)
linear_speed = ___
angular_speed = ___
```

---

### Шаг 3: Получите состояние клавиш

После обнуления скоростей:

```python
# TODO: Получаем состояние всех клавиш
keys = ___
```

**Подсказка:** Используйте `pygame.key.get_pressed()`

---

### Шаг 4: Управление линейной скоростью

Теперь проверяем клавиши W и S:

```python
# TODO: Управление линейной скоростью
# Сначала проверьте конфликт: если W и S нажаты одновременно
if ___:
    linear_speed = 0
# Затем проверьте W отдельно (используйте elif!)
elif ___:
    linear_speed = 150
# Затем проверьте S отдельно (используйте elif!)
elif ___:
    linear_speed = -100
```

**Важно:** Используйте `elif`, а не просто `if`, чтобы проверки были взаимоисключающими!

---

### Шаг 5: Управление угловой скоростью

Для поворотов A и D конфликта обычно нет, но можно обработать аналогично:

```python
# TODO: Управление угловой скоростью
# Можете сделать проверку A+D для полноты, или просто две отдельные проверки
if ___:
    angular_speed = 120  # Влево (против часовой)
if ___:
    angular_speed = -120  # Вправо (по часовой)
```

**Вопрос для размышления:** Что произойдёт, если нажать A и D одновременно с этим кодом?

---

### Шаг 6: Тестирование

Остальной код игрового цикла остаётся без изменений:
- Обновление направления через `angular_speed * dt`
- Обновление позиции через векторное движение
- Рисование робота и информации

Запустите программу и проверьте управление!

---

## ✅ Проверка

### Базовые проверки:
- ✅ **W** — робот едет вперёд, `linear_speed = 150`
- ✅ **S** — робот едет назад, `linear_speed = -100`
- ✅ **A** — робот поворачивается влево, `angular_speed = 120`
- ✅ **D** — робот поворачивается вправо, `angular_speed = -120`
- ✅ **Отпустили все** — робот останавливается, все скорости = 0

### Проверка конфликтов:
- ✅ **W + S** — робот НЕ движется (linear_speed = 0)
- ✅ **A + D** — что происходит? (зависит от вашей реализации)

### Комбинированные проверки:
- ✅ **W + A** — робот едет вперёд и поворачивает влево (дуга влево)
- ✅ **W + D** — робот едет вперёд и поворачивает вправо (дуга вправо)
- ✅ **S + A** — робот едет назад с поворотом влево
- ✅ **S + D** — робот едет назад с поворотом вправо

### Проверка информации:
- ✅ Координаты меняются плавно при движении
- ✅ Направление меняется при поворотах
- ✅ Скорости отображаются правильно

---

## 🎮 Эксперименты

### Эксперимент 1: Более быстрое управление

Попробуйте увеличить скорости:

```python
if keys[pygame.K_w]:
    linear_speed = 250  # Было 150

if keys[pygame.K_a]:
    angular_speed = 180  # Было 120
```

Какие значения кажутся наиболее комфортными?

---

### Эксперимент 2: Симметричные скорости

Сделайте движение назад таким же быстрым, как вперёд:

```python
elif keys[pygame.K_s]:
    linear_speed = -150  # Вместо -100
```

---

### Эксперимент 3: Медленный режим (сложно)

Добавьте проверку Shift для замедления:

```python
# После получения keys, но перед проверкой W/A/S/D:
speed_multiplier = 0.5 if keys[pygame.K_LSHIFT] else 1.0

# Потом при установке скоростей:
if keys[pygame.K_w]:
    linear_speed = 150 * speed_multiplier
```

---

## 🐛 Частые ошибки

### Ошибка 1: Робот не останавливается
**Симптом:** Робот продолжает двигаться после отпускания клавиш.

**Причина:** Забыли обнулить скорости в начале кадра.

**Решение:** Убедитесь, что `linear_speed = 0` и `angular_speed = 0` есть **перед** проверкой клавиш.

---

### Ошибка 2: Робот движется рывками
**Симптом:** Движение не плавное, робот дёргается.

**Причина:** Забыли умножить скорость на `dt`.

**Решение:** Проверьте обновление координат: `robot_x += linear_speed * cos(...) * dt`

---

### Ошибка 3: W + S не обрабатывается
**Симптом:** При одновременном нажатии W и S робот движется.

**Причина:** Проверка конфликта стоит не первой, или используется `if` вместо `elif`.

**Решение:** Проверка `if keys[K_w] and keys[K_s]` должна быть **первой**, остальные проверки через `elif`.

---

## 🎯 Итоги задания

Поздравляем! Вы создали полноценное управление роботом через скорости!

**Что теперь работает:**
- ✅ Физически корректное движение через скорости
- ✅ На экране отображается: позиция, направление, скорости
- ✅ Управление WASD (движение и поворот)
- ✅ Обработка конфликтов (W+S)
- ✅ Комбинированное управление (можно одновременно ехать и поворачивать)
- ✅ Робот останавливается при отпускании клавиш

**Что вы изучили:**
- Разницу между прямым изменением координат и управлением через скорости
- Использование delta time для плавного движения
- Проверку состояния клавиш в Pygame
- Обнуление состояния перед проверкой ввода
- Обработку конфликтующих команд
- Векторное движение (cos/sin для направления)

---

## 🚀 Что дальше?

В следующем часе мы:
- Добавим управление мышью (клик для телепортации)
- Научим робота следовать за курсором
- Сделаем автоматический поворот к цели

Готовы продолжить? 🎮