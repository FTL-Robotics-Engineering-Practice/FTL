# Часть 4.1: Теория математического маятника

## 1. Уравнение движения маятника

### Физическая модель

Рассмотрим **математический маятник**:

- Материальная точка массой $m$ (грузик)
- Подвешена на невесомой нерастяжимой нити длиной $L$
- Движется в вертикальной плоскости под действием силы тяжести

```
        ● ← точка подвеса
        |
        |  L (длина нити)
        |
        ● ← грузик массы m
```

**Угол отклонения** $\theta$ отсчитывается от вертикали (положения равновесия):

- $\theta = 0$ — маятник висит вертикально вниз
- $\theta > 0$ — отклонение вправо
- $\theta < 0$ — отклонение влево

### Вывод уравнения движения

Применим **второй закон Ньютона для вращательного движения**:

$$
I \cdot \theta'' = M
$$

где:

- $I$ — момент инерции относительно точки подвеса
- $\theta''$ — угловое ускорение
- $M$ — момент силы

**Шаг 1:** Момент инерции материальной точки

$$
I = m \cdot L^2
$$

**Шаг 2:** Момент силы тяжести

Сила тяжести $\vec{F} = m\vec{g}$ направлена вертикально вниз.
Тангенциальная составляющая (перпендикулярная нити): $F_t = -mg\sin(\theta)$
Момент силы: $M = F_t \cdot L = -mgL\sin(\theta)$

Знак "минус" потому что сила стремится вернуть маятник к положению равновесия.

**Шаг 3:** Подставляем в уравнение движения

$$
\begin{align*}
mL^2 \cdot \theta'' &= -mgL\sin(\theta) \\
\theta'' &= -\frac{g}{L} \cdot \sin(\theta)
\end{align*}
$$

Это **нелинейное дифференциальное уравнение второго порядка**.

### Приближение малых углов

Для малых углов $|\theta| < 0.3$ рад $\approx 17°$ справедливо:

$$
\sin(\theta) \approx \theta
$$

Тогда уравнение упрощается:

$$
\theta'' = -\frac{g}{L} \cdot \theta
$$

Это **уравнение гармонических колебаний** с решением:

$$
\theta(t) = A \cos(\omega t + \varphi)
$$

где:

- $A$ — амплитуда (максимальный угол отклонения)
- $\omega = \sqrt{g/L}$ — **циклическая частота**
- $\varphi$ — начальная фаза

**Период колебаний**:

$$
T = \frac{2\pi}{\omega} = 2\pi\sqrt{\frac{L}{g}}
$$

**Важно:** Период **не зависит** от амплитуды (для малых углов)!

---

## 2. Метод Эйлера для численного решения

### Зачем нужны численные методы?

Полное уравнение $\theta'' = -\frac{g}{L} \sin(\theta)$ **не решается аналитически** в элементарных функциях.

Для произвольных углов (не малых) нужно использовать **численные методы**.

### Идея метода Эйлера

**Производная** — это скорость изменения функции:

$$
f'(t) = \lim_{\Delta t \to 0} \frac{f(t + \Delta t) - f(t)}{\Delta t}
$$

Для малого, но конечного $\Delta t$:

$$
f'(t) \approx \frac{f(t + \Delta t) - f(t)}{\Delta t}
$$

Отсюда:

$$
f(t + \Delta t) \approx f(t) + f'(t) \cdot \Delta t
$$

Это и есть **формула Эйлера**.

### Применение к маятнику

У нас уравнение второго порядка: $\theta'' = -\frac{g}{L} \sin(\theta)$

Введем **угловую скорость** $\omega = \theta'$.

Получаем **систему двух уравнений первого порядка**:

$$
\begin{cases}
\theta' = \omega \\
\omega' = -\frac{g}{L} \sin(\theta)
\end{cases}
$$

### Метод Эйлера: шаг за шагом

Пусть известны значения $\theta$ и $\omega$ в момент времени $t$.

**Шаг 1:** Вычисляем производные в текущий момент:

- $\theta' = \omega$ (угловая скорость)
- $\omega' = -\frac{g}{L} \sin(\theta)$ (угловое ускорение)

**Шаг 2:** Обновляем значения через малый промежуток времени $dt$:

$$
\begin{align*}
\theta_{\text{new}} &= \theta_{\text{old}} + \omega_{\text{old}} \cdot dt \\
\omega_{\text{new}} &= \omega_{\text{old}} - \frac{g}{L} \sin(\theta_{\text{old}}) \cdot dt
\end{align*}
$$

**Шаг 3:** Повторяем процесс для следующего шага.

### Выбор шага времени $dt$

**Важно:** Чем меньше $dt$, тем точнее решение!

Но слишком маленький $dt$ → много вычислений, программа медленная.

**Практическое правило:**

$$
dt \ll \frac{T}{100}
$$

где $T$ — период колебаний.

Для маятника с $L = 1$ м, $g = 9.81$ м/с²:

- $T \approx 2$ с
- Хороший выбор: $dt = 0.01$ с (100 шагов на период)

**В нашей программе** с частотой обновления 60 FPS:

$$
dt = \frac{1}{60} \approx 0.0167 \text{ с}
$$

Это вполне достаточно для точности!

### Пример расчета на бумаге

**Начальные условия:**

- $L = 1$ м, $g = 9.81$ м/с²
- $\theta_0 = 0.2$ рад ($\approx 11.5°$)
- $\omega_0 = 0$ рад/с (отпустили без начальной скорости)
- $dt = 0.1$ с

**Шаг 0** ($t = 0$):

- $\theta = 0.2$ рад
- $\omega = 0$ рад/с

**Шаг 1** ($t = 0.1$ с):

- $\omega' = -\frac{9.81}{1} \sin(0.2) = -9.81 \times 0.1987 \approx -1.949$ рад/с²
- $\theta_{\text{new}} = 0.2 + 0 \times 0.1 = 0.2$ рад
- $\omega_{\text{new}} = 0 + (-1.949) \times 0.1 = -0.1949$ рад/с

**Шаг 2** ($t = 0.2$ с):

- $\theta_{\text{new}} = 0.2 + (-0.1949) \times 0.1 = 0.18051$ рад
- $\omega' = -9.81 \times \sin(0.18051) \approx -1.759$ рад/с²
- $\omega_{\text{new}} = -0.1949 + (-1.759) \times 0.1 = -0.3708$ рад/с

И так далее... Маятник начинает качаться!

### Метод Верле (Verlet): улучшенная точность

**Проблема метода Эйлера:** Он **не сохраняет энергию**! При долгой симуляции маятник может либо раскачиваться всё сильнее, либо затухать, хотя трения нет.

**Метод Верле** — более точный метод, который **лучше сохраняет энергию**.

#### Идея метода

Вместо использования только первой производной, используем **вторую производную** (ускорение) напрямую!

Разложение Тейлора для $\theta(t)$:

$$
\theta(t + dt) \approx \theta(t) + \theta'(t) \cdot dt + \frac{1}{2}\theta''(t) \cdot dt^2
$$

Аналогично для скорости $\omega(t)$:

$$
\omega(t + dt) \approx \omega(t) + \omega'(t) \cdot dt + \frac{1}{2}\omega''(t) \cdot dt^2
$$

Но $\omega''$ нам не известно. Используем **полушаговую схему (Velocity Verlet)**:

#### Метод Velocity Verlet: формулы

**Шаг 1:** Вычисляем текущее ускорение

$$
\alpha_{\text{old}} = -\frac{g}{L} \sin(\theta_{\text{old}})
$$

**Шаг 2:** Обновляем позицию с учетом ускоренвия

$$
\theta_{\text{new}} = \theta_{\text{old}} + \omega_{\text{old}} \cdot dt + \frac{1}{2}\alpha_{\text{old}} \cdot dt^2
$$

**Шаг 3:** Вычисляем новое ускорение

$$
\alpha_{\text{new}} = -\frac{g}{L} \sin(\theta_{\text{new}})
$$

**Шаг 4:** Обновляем скорость как среднее между старым и новым ускорением

$$
\omega_{\text{new}} = \omega_{\text{old}} + \frac{1}{2}(\alpha_{\text{old}} + \alpha_{\text{new}}) \cdot dt
$$

#### Сравнение методов

| Свойство                              | Метод Эйлера             | Метод Верле           |
| --------------------------------------------- | ----------------------------------- | ------------------------------- |
| **Порядок точности**     | O(dt)                               | O(dt²)                         |
| **Сохранение энергии** | Плохое                        | Хорошее                  |
| **Вычислений на шаг**    | 1 вычисление sin          | 2 вычисления sin      |
| **Подходит для**             | Быстрых прототипов | Точных симуляций |

**Вывод:**

- Для учебного проекта можем начать с **метода Эйлера** (проще)
- Для **точной симуляции** лучше использовать **метод Верле**
- При одинаковом $dt$ метод Верле даст **значительно меньшую ошибку**

#### Пример кода (Python)

**Метод Эйлера:**

```python
# Текущее состояние: theta, omega
alpha = -(g/L) * math.sin(theta)
theta += omega * dt
omega += alpha * dt
```

**Метод Верле:**

```python
# Текущее состояние: theta, omega
alpha_old = -(g/L) * math.sin(theta)
theta += omega * dt + 0.5 * alpha_old * dt**2
alpha_new = -(g/L) * math.sin(theta)
omega += 0.5 * (alpha_old + alpha_new) * dt
```

Всего **3 строки кода**, но намного точнее!

---

## 3. Объяснение параметров

### Основные переменные

| Параметр                                                 | Обозначение | Единица измерения | Физический смысл                                                                                          |
| ---------------------------------------------------------------- | ---------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| **Длина нити**                                    | $L$                  | метры (м)                   | Расстояние от точки подвеса до центра массы грузика                          |
| **Ускорение свободного падения** | $g$                  | м/с²                           | На Земле$\approx 9.81$ м/с², на Луне $\approx 1.62$ м/с²                                          |
| **Угол отклонения**                          | $\theta$             | радианы (рад)           | Угол между нитью и вертикалью.**ВАЖНО:** В радианах, не градусах! |
| **Угловая скорость**                        | $\omega$             | рад/с                         | Скорость изменения угла$\omega = \theta'$                                                         |
| **Шаг времени**                                  | $dt$                 | секунды (с)               | Промежуток времени между обновлениями в симуляции                            |

### Важные замечания

**1. Углы в радианах**

В программировании тригонометрические функции работают с **радианами**!

Перевод:

- $180° = \pi$ рад
- $1° = \frac{\pi}{180} \approx 0.01745$ рад
- $1$ рад $= \frac{180°}{\pi} \approx 57.3°$

**Пример:** $\theta = 0.2$ рад $\approx 11.5°$

**2. Начальные условия**

Для запуска симуляции нужно задать:

- $\theta_0$ — начальное отклонение
- $\omega_0$ — начальная угловая скорость (обычно 0, если отпускаем из покоя)

**3. Период и частота**

Для маятника без трения:

$$
T = 2\pi\sqrt{\frac{L}{g}}
$$

**Примеры:**

| $L$ (м) | $g$ (м/с²) | $T$ (с) | Частота (Гц) |
| ---------- | --------------- | ---------- | --------------------- |
| 1.0        | 9.81            | 2.01       | 0.50                  |
| 0.5        | 9.81            | 1.42       | 0.70                  |
| 2.0        | 9.81            | 2.84       | 0.35                  |
| 1.0        | 1.62 (Луна) | 4.93       | 0.20                  |

**4. Связь угловой и линейной скорости**

Если грузик движется по окружности радиусом $L$:

$$
v = L \cdot \omega
$$

где $v$ — линейная скорость (м/с).

**5. Энергия маятника**

Полная механическая энергия (для малых углов):

$$
E = \frac{1}{2} m L^2 \omega^2 + \frac{1}{2} m g L \theta^2
$$

- Первое слагаемое — кинетическая энергия
- Второе слагаемое — потенциальная энергия

Для маятника **без трения** энергия сохраняется!

---

## Резюме

**Что узнали:**

1. Уравнение движения маятника: $\theta'' = -\frac{g}{L} \sin(\theta)$
2. Для малых углов: $\theta'' \approx -\frac{g}{L} \theta$ → гармонические колебания
3. Период: $T = 2\pi\sqrt{L/g}$ (не зависит от амплитуды для малых углов!)
4. Метод Эйлера: численное решение через конечные разности
5. Система первого порядка: $\theta' = \omega$, $\omega' = -\frac{g}{L}\sin(\theta)$

**Что дальше:**

- Напишем программу на Python с pygame
- Визуализируем движение маятника
- Построим графики $\theta(t)$ через Plotly
- Проведем эксперименты: измерим период, найдем $g$ и $L$

**Готовы к программированию? Вперед!** 🚀
